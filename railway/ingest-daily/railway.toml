# Railway cron: full ingest daily 06:15 UTC.
# Service root directory: railway/ingest-daily

[build]
builder = "DOCKERFILE"
dockerfilePath = "../../Dockerfile"

[deploy]
cronSchedule = "15 6 * * *"
restartPolicyType = "NEVER"
