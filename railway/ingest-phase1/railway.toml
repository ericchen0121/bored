# Railway cron: Phase 1 scrapers every 6h (UTC).
# Service root directory: railway/ingest-phase1

[build]
builder = "DOCKERFILE"
dockerfilePath = "../../Dockerfile"

[deploy]
cronSchedule = "0 */6 * * *"
restartPolicyType = "NEVER"
