# Shared build settings. Configure health checks per service in the Railway dashboard
# (API: GET /health). Set SERVICE=api|web|ingest on each service.

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
